# -*- coding: utf-8 -*-

from ariadne import ObjectType

import logging

logger = logging.getLogger(__name__)

def resolvers():
    # TODO need some resolvers if relationship field names differ from the
    # names of the relationships in ORM
    return [
    ]
